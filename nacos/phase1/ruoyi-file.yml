server:
  port: 9300

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

file:
  prefix: /profile
  path: D:/ruoyi/uploadPath
  domain: https://zoumh.com

referer:
  enabled: false
  allowed-domains: []

minio:
  url: ${MINIO_URL:https://s3.cstcloud.cn}
  accessKey: ${MINIO_ACCESS_KEY:AKIAKPVAL7O24XRGWFSP}
  secretKey: ${MINIO_SECRET_KEY:Y=F+7+KX085FNQK8YA5T30PK9LE/BZPPFT/5X/2W}
  bucketName: ${MINIO_BUCKET:zoumh}
  autoCreateBucket: ${MINIO_AUTO_CREATE_BUCKET:false}
  region: ${MINIO_REGION:us-east-1}
  manageBucketPolicy: ${MINIO_MANAGE_BUCKET_POLICY:false}
  domain: ${MINIO_DOMAIN:https://s3.cstcloud.cn}
